[project]
name = "search-kernel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "elasticsearch==8.15.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
